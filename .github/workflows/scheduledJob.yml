name: Cron Actions
on: 
  schedule:
  - cron: '* * * * *'

jobs:
  cronjob:
    name: Cron job
    runs-on: ubuntu-latest

    steps:
    - name: Echo text
      run: |
         echo "This is a cron job"
